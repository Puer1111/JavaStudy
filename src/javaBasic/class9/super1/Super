**Super**

부모를 통해 자식이 오버라이딩해서 같은 메소드명을 가지고 있거나 같은 변수 명을 가지고 있을 경우 일반적으로 부모의 메소드 호출은 불가능하다.
그러나 Super 를 붙히게 되면 부모 요소를 선택하기에 호출 할 수 있다.

**Super 생성자**
상속관계의 인스턴스 생성시 메모리 내부에 자식과 부모 클래스 모두 만들어진다.
Child 생성 시 Parent도 만들어진다.

