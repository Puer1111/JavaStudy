**toString()

기본적으로 객체의 참조값을 반환한다.

-------------------------------------------------------------------------------------------------------------------

** System.out.println()

아무곳에서나 println 을 쓸수 있는 이유는 다형적 참조와 메소드 오버라이딩 덕분이다.
println 는 Object 타입의 매개변수를 사용하고 내부에서 toString 을 호출 할 수 있기 때문에 toString 을 직접 쓰지 않고도 편하게 출력이 가능한 것.

-------------------------------------------------------------------------------------------------------------------
** 의존관계

정적:컴파일 시간에 결정되며 , 클래스간의 관계를 의미한다.

동적:프로그램을 실행하는 런타임에 확인 할 수 있는 의존관계. 프로그램이 실행되어 봐야 어떤 인스턴스가 넘어오고 사용하는지를 알 수 있다.