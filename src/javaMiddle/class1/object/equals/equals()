자바는 두 객체가 같다는걸 2가지로 분류함

동일성(Identity) : == 를 사용해서 두 객체의 참조가 동일 한 객체를 가리킨다.
동등성(Equality) : equals()메서드를 사용해 두 객체가 논리적으로 동등한지 확인

동일 은 정말 완전히 똑같은거고 동등은 가치나 수준을 의미하지만 형태나 외관이 다를 수 있다.

ex) User a = new User("id-10");
ex) User b = new User("id-10");

이 둘은 다른 객체지만 논리적으로는 같은 회원이다. 따라서 동일성은 X 동등성은 O 이다.

ex) String a = "hello";
ex) String b = "hello";

같은 케이스.